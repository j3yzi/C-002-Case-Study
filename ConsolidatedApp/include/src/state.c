#include "../headers/state.h"

// Define the global state variable
appState employeeListCreated = {0}; // Initialize isEnabled to false
appState studentListCreated = {0}; // Initialize isEnabled to false  
appState universityEmployeeListCreated = {0}; // Initialize isEnabled to false
